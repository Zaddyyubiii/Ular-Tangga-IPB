namespace Core
{
    public enum GameState
    {
        MainMenu,
        Prologue,
        WaitingForInput,
        Rolling,
        Moving,
        ResolvingTile,
        ChangingTurn,
        GameOver
    }
}
